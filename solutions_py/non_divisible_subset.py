from solutions_py.harness import Harness


class NonDivisibleSubset(Harness):
    """
    https://www.hackerrank.com/challenges/non-divisible-subset/problem?utm_campaign=challenge-recommendation&utm_medium=email&utm_source=24-hour-campaign
    Given a set of distinct integers, print the size of a maximal subset of `S` where the sum of any `2` numbers in is `S'`not evenly divisible by `k`.
    """
    def solution(self):
        pass

    @staticmethod
    def _test_cases() -> list:
        """
        Format: dictionary item with 'kwargs' for inputs
        :return: list
        """
        return [
            # {'kwargs': {'m': 4, 'arr': [1, 4, 5, 3, 2]}, 'expected': [1, 4]},
        ]
